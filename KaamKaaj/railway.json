{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "JAVA_HOME=$(dirname $(dirname $(readlink -f $(which java)))) && export JAVA_HOME && echo \"JAVA_HOME=$JAVA_HOME\" && mvn clean package -DskipTests --no-transfer-progress",
    "nixpacksPlan": {
      "providers": ["java"],
      "variables": {
        "NIXPACKS_JDK_VERSION": "21"
      }
    }
  },
  "deploy": {
    "startCommand": "java -Xmx400m -jar target/kaamkaaj-0.0.1-SNAPSHOT.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}